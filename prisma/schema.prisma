datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Wallet {
  id    Int     @id @default(autoincrement())
  idUser Int
  name  String
  solde Int @default(0)

}
